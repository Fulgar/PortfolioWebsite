import React from 'react';
import PropTypes from 'prop-types';
import './NavBar.css';

const NavBar = () => (
  <div className="NavBar">
      <b>NavBar is here</b>
  </div>
);

NavBar.propTypes = {};

NavBar.defaultProps = {};

export default NavBar;

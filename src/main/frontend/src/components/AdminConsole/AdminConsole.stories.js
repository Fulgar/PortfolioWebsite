/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import AdminConsole from './AdminConsole';

storiesOf('AdminConsole', module).add('default', () => <AdminConsole />);

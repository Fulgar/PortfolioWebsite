/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import ProjectPage from './ProjectPage';

storiesOf('ProjectPage', module).add('default', () => <ProjectPage />);
